# NumPy_Practice_Repository
This repository contains my **NumPy practice files and notebooks**, created while learning and strengthening my understanding of numerical computing in Python.
The main goal of this repository is to practice **core NumPy concepts** through hands-on examples, experiments, and small exercises that are commonly used in **data analysis, scientific computing, and machine learning**.

---

## 📌 Topics Covered

- Introduction to NumPy
- NumPy Arrays (`ndarray`)
- Array creation methods  
  (`array`, `zeros`, `ones`, `arange`, `linspace`)
- Array attributes  
  (`shape`, `dtype`, `ndim`, `size`)
- Indexing and slicing (1D & 2D arrays)
- Boolean and fancy indexing
- Array operations and broadcasting
- Mathematical and statistical functions  
  (`sum`, `mean`, `min`, `max`, `std`, etc.)
- Shape manipulation  
  (`reshape`, `flatten`, `ravel`, `transpose`)
- Handling missing values (`NaN`)
- Random number generation
- Basic file operations with NumPy

---
